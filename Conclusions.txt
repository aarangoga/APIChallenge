#1. A new user can be created taking into account that the ID. Cannot be duplicated
#2. This user can be search with a GET and updated using a PUT, just using the name of the user to be easy to do it for the user
#3. Each method was created in a different files because is gonna be more reusable and it can be refactor in a better way for future needs.